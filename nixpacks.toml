[phases.setup]
nixPkgs = ["python312Full", "postgresql"]
aptPkgs = ["python3-pip"]

[phases.install]
dependsOn = ["setup"]
cmds = ["pip install --upgrade pip", "pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
